{
  "name": "awax",
  "version": "1.0.0",
  "description": "Website for AWAX",
  "main": "index.js",
  "scripts": {
    "devserver": "live-server",
    "scss": "node-sass sass/main.scss css/style.css -w",
    "prefix": "postcss --use autoprefixer -b 'last 10 versions' css/style.css -o css/style.css",
    "build": "npm-run-all scss prefix"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "autoprefixer": "^9.0.2",
    "node-sass": "^7.0.0",
    "npm-run-all": "^4.1.3",
    "postcss-cli": "^6.0.0"
  },
  "dependencies": {
    "node-sass": "^7.0.0"
  }
}
